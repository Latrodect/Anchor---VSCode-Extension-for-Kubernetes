// Placeholder implementation for generating reviewer suggestions
export function generateReviewerSuggestions(selectedText: string): string[] {
    return [
        'Consider using a more descriptive variable name.',
        'Avoid nested loops to improve code readability.',
        'Add comments to explain complex logic.',
        'Use consistent indentation throughout the code.',
    ];
}
